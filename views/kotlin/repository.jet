package com.gowoobro.gymspring.repository

import com.gowoobro.gymspring.entity.{{.ModelName}}
import com.gowoobro.gymspring.entity.{{.ModelName}}CreateRequest
import com.gowoobro.gymspring.entity.{{.ModelName}}UpdateRequest
import org.springframework.data.domain.Page
import org.springframework.data.domain.Pageable
import org.springframework.data.jpa.repository.JpaRepository
import org.springframework.data.jpa.repository.Query
import org.springframework.stereotype.Repository
{{if .HasDateTime}}import java.time.LocalDateTime{{end}}
{{if .HasDecimal}}import java.math.BigDecimal{{end}}
{{range .Enums}}import com.gowoobro.gymspring.enums.{{.EnumPackage}}.{{.Name|title}}
{{end}}
{{allJoins := .Joins}}
@Repository
interface {{.ModelName}}Repository : JpaRepository<{{.ModelName}}, Long> {
    @Query("SELECT m FROM {{.ModelName}} m{{range allJoins}} LEFT JOIN FETCH m.{{.Alias|lower}}{{end}}")
    override fun findAll(pageable: Pageable): Page<{{.ModelName}}>

    @Query("SELECT m FROM {{.ModelName}} m{{range allJoins}} LEFT JOIN FETCH m.{{.Alias|lower}}{{end}} WHERE m.id = :id")
    override fun findById(id: Long): java.util.Optional<{{.ModelName}}>
{{range .Columns }}{{joinData := findJoinByColumn(.DBName, allJoins)}}{{ if .Name != "Id"}}{{"\n"}}{{if not joinData}}    @Query("SELECT m FROM {{.ModelName}} m{{range allJoins}} LEFT JOIN FETCH m.{{.Alias|lower}}{{end}} WHERE m.{{.Name|lower}} = :{{.Name|lower}}")
    fun findBy{{.Name}}WithJoin({{.Name|lower}}: {{if .IsEnum}}{{.EnumType}}{{else}}{{.KotlinType}}{{end}}): List<{{.ModelName}}>
{{end}}{{if joinData}}    @Query("SELECT m FROM {{.ModelName}} m{{range allJoins}} LEFT JOIN FETCH m.{{.Alias|lower}}{{end}} WHERE m.{{joinData.ColumnName}}Id = :{{joinData.Alias|lower}}")
    fun findBy{{.Name}}WithJoin({{joinData.Alias|lower}}: Long): List<{{.ModelName}}>
{{end}}{{- end}}{{end}}}